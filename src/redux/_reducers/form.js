import { reducer as form } from "redux-form";

/**
 * Reducer neccessary to track events related to forms within the system.
 * The package "redux-form" allows to track state like touched, dirty and empty
 * within redux.
 */
export default form;
